# Songs

Drop your audio files here so the Lofi Zone player can find them:

- `forest.mp3`
- `sunset.mp3`
- `moonlight.mp3`

The player references them at `/songs/<name>.mp3`. Replace the file names in
`src/routes/main/Therapists/Home/LofiZone/+page.svelte` if you use different ones.